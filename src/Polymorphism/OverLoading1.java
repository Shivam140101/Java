package Polymorphism;

/*Change the return type  but arguments are same  in function
OverLoading */

/*
public class OverLoading1 {
    int  show(int x ){
        System.out.println("int  x  "+x);
        return 100;
    }
    void show(int  x ){
        System.out.println("int  x  "+x);
    }


    static public void  main(String ...s) {
        OverLoading1 obj = new OverLoading1();
        obj.show(10);
    }
}
*/

/* Rule:=>  The function Oveloading is always achieve to
change the arguments of the function does not matter return
of function  it uiquely identify the arguments are different

The function overloading can't be achieve to change the return
of function only
  Its only and only arguments are different
*/